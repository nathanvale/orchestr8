# Vercel Remote Cache Configuration
# These environment variables should be set in your CI/CD environment
# For GitHub Actions, set these as repository secrets/variables

# Your Vercel team identifier (can be public)
TURBO_TEAM=your-team-id

# Your Vercel token for remote cache access (keep secret)
TURBO_TOKEN=your-vercel-token

# Optional: HMAC signature key for artifact verification (keep secret)
TURBO_REMOTE_CACHE_SIGNATURE_KEY=your-signature-key

# Optional: SCM base for incremental builds in CI
TURBO_SCM_BASE=main