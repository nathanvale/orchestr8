# npm configuration for noise reduction
loglevel=warn

# pnpm settings
auto-install-peers=true
strict-peer-dependencies=false

# Performance settings
prefer-offline=true
network-timeout=30000

# Registry settings
registry=https://registry.npmjs.org/