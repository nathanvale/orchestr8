// This will trigger the Claude hook when saved
const x = unknownVariable // TypeScript error

const num: number = 'string' // Type error

// This should get auto-formatted
const messy = 'spacing'
console.log(x, num, messy) // Use the variables to avoid lint errors
