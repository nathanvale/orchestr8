// Simple test file with fixable formatting issues

const value = 42 // Extra spaces
const name = 'test' // Missing spaces around =

// Missing semicolon
const data = 'hello'

// Bad indentation
function test() {
  console.log('bad indent')
}

export { value, name, data, test }
