#!/usr/bin/env node
/**
 * CLI executable for quality-check
 */

import { runCLI } from '../dist/facades/cli.js'

// Run CLI with process arguments
runCLI(process.argv.slice(2)).catch(error => {
  console.error('Fatal error:', error)
  process.exit(1)
})