{
  "extends": "../../tooling/tsconfig/library.json",
  "compilerOptions": {
    // Library-specific overrides (library.json already has most settings)
    "types": ["node"]
  },
  "include": ["src/**/*"],
  "exclude": ["**/*.test.*", "**/*.spec.*", "tests/**/*", "tsup.config.ts"]
}
